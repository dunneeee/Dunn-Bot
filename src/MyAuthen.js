import { Authen } from "fca-dunnn-bot";

class MyAuthen extends Authen {
  constructor() {
    super();
    // write some function to authenticate before run command then use this.add() to add it
  }
}

export default MyAuthen;
